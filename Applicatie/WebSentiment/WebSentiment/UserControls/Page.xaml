<UserControl x:Name="PageUserControl"
    x:Class="WebSentiment.UserControls.Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WebSentiment.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Style="{ThemeResource AppFontStyleStandard}"
    >

    <Grid x:Name="pageGrid" >
                <Grid.RowDefinitions>
            <RowDefinition Height=".1*"></RowDefinition>
                    <RowDefinition Height=".9*"></RowDefinition>
                </Grid.RowDefinitions>
        <StackPanel Background="{ThemeResource AppBackgroundGrey}" x:Name="spHeader" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="0"></StackPanel>
        <StackPanel Background="{ThemeResource AppBackgroundWhite}" x:Name="spActivity" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="1"></StackPanel>
            </Grid>
            
</UserControl>
